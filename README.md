# data_mining
# Academic Performance Analysis and Prediction System

## 📋 Project Overview
Web-based interactive data mining system for analyzing student academic data and predicting academic performance.

## 🚀 Quick Start

### 1. Install Dependencies
```bash
pip install -r requirements.txt
Clone the repository (if applicable) or download the project files.

Create a virtual environment (optional but recommended):

bash
python -m venv venv
Activate the virtual environment:

On Windows:

bash
venv\Scripts\activate
On macOS/Linux:

bash
source venv/bin/activate
Install the required packages:

bash
pip install streamlit pandas numpy scikit-learn matplotlib seaborn mlxtend plotly joblib

How to Run
Ensure you are in the project directory (where app.py is located).

Run the Streamlit app:

bash
streamlit run app.py